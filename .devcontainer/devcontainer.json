{
	"name": "Cargo",
	"image": "mcr.microsoft.com/devcontainers/base:jammy",
	"onCreateCommand": "curl https://sh.rustup.rs -sSf | sh -s -- --default-toolchain nightly-2025-05-23 -y",
	"postCreateCommand": ". $HOME/.cargo/env && rustup component add rust-src && cargo install cargo-psx --version 0.1.4"
}
